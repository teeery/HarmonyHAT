/**
 * 第六幕：AI 调研员 — 让成果继续生长（约 2 分钟）
 *
 * 目标：展示这是一个"可持续研究平台"，不只是网页
 *
 * 布局：
 *   左侧 45% — AI 头像 + 名称 + 能力标签
 *   右侧 55% — 对话区（预设问题 + 对话流 + 输入框）
 *
 * 知识来源（必须绑定团队数据）：
 *   访谈记录 + 照片 + 调研笔记 + 问卷数据 + 政策文件 + 鸿蒙资料
 *     → 知识库 → AI 助手
 *
 * 预设问题：
 *   "最大的发现是什么？"
 *   "为什么选择南城？"
 *   "鸿蒙解决了什么具体问题？"
 *
 * 技术：React Chat UI + RAG 后端 + SSE 流式输出
 */
export function AIResearcher() {
  return (
    <section className="relative min-h-screen w-full bg-[var(--color-bg-deep)]">
      {/* TODO: 左侧 — AI 头像区 + 能力标签 */}
      {/* TODO: 右侧 — 对话 UI + 预设问题 + 输入框 */}
    </section>
  )
}
