/**
 * 第二幕：走进南城 — 探索地图（约 1 分钟）
 *
 * 目标：告诉用户——我们不是凭空想象，我们真的走进了城市
 *
 * 布局：
 *   左侧 60% — 3D 地图 / 简化城市模型 + 四个探索节点
 *   右侧 40% — 引导文字 + 节点信息 + 进入按钮
 *
 * 节点：
 *   ● 产业观察点    ● 社区观察点
 *   ● 商业观察点    ● 公共服务点
 *
 * 技术：Mapbox GL JS 或 Three.js 简化城市模型
 */
export function ExploreMap() {
  return (
    <section className="relative h-screen w-full bg-[var(--color-bg-deep)]">
      {/* TODO: 左侧 — 3D 地图 + 四个节点标记 */}
      {/* TODO: 右侧 — 引导文字 + 节点详情面板 */}
    </section>
  )
}
