/**
 * 第三幕：田野调查 — 发现卡片（约 2-3 分钟）
 *
 * 目标：对应真实实践，不做照片墙——用"发现卡片"三层结构
 *
 * 每张卡片：左照片 | 右三层文字（观察 → 思考 → 未来方向）
 *
 * 四张卡片：
 *   卡片 1：社区 — 数字服务门槛
 *   卡片 2：企业 — 数据无法流动
 *   卡片 3：商业 — 线上线下割裂
 *   卡片 4：政务 — 信息不透明
 *
 * 交互：垂直滚动切换卡片，右侧竖线指示器
 *
 * 技术：React + Framer Motion + Intersection Observer
 */
export function FieldResearch() {
  return (
    <section className="relative w-full bg-[var(--color-bg-deep)]">
      {/* TODO: 四张全屏发现卡片，垂直滚动 */}
      {/* TODO: 右侧滚动指示器（竖线 + 圆点） */}
    </section>
  )
}
