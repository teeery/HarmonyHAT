/**
 * 第七幕：未来南城 2035 — 青年想象（终章高潮）
 *
 * 目标：不预测未来，展示设计思考
 *
 * 四个阶段（滚动推进时间线）：
 *   A — 现在南城（2026 年夏天看到的城市）
 *   B — 过渡（年份跳动 2026→2035，城市渐变）
 *   C — 2035 南城（三大主题高亮：智慧交通/社区/教育）
 *   D — 终场（zoom out → 光点 → 黑屏字幕）
 *
 * 终场文案：
 *   "我们调研的不只是今天的鸿蒙。"
 *   "而是未来十年，人与城市连接的方式。"
 *
 * 技术：Three.js 城市场景 + ScrollTrigger 时间线驱动 + 粒子系统 + bloom 后处理
 */
export function FutureCity() {
  return (
    <section className="relative h-screen w-full bg-[var(--color-bg-deep)] overflow-hidden">
      {/* TODO: Three.js Canvas + GSAP ScrollTrigger 滚动驱动时间线 */}
    </section>
  )
}
