/**
 * 粒子场 — 数据流可视化
 *
 * 用于序章的数据流线和未来南城的空中数据层
 *
 * 参数：
 *   - particleCount: 粒子数量
 *   - speed: 流动速度
 *   - color: 粒子颜色（默认科技蓝）
 */
export function ParticleField() {
  return (
    <div className="absolute inset-0 pointer-events-none">
      {/* TODO: Three.js 粒子系统（BufferGeometry + Points） */}
    </div>
  )
}
